# Copy this file to .env and fill in your values
# .env is gitignored — never commit real credentials

# Override config/test-config.json values via environment (optional)
# If set, these take priority over test-config.json

MARKETPLACE_BASE_URL=http://localhost:3000

CUSTOMER_EMAIL=test-customer@example.com
CUSTOMER_PASSWORD=TestPass123!

SELLER_EMAIL=test-seller@example.com
SELLER_PASSWORD=TestPass123!

ADMIN_EMAIL=test-admin@example.com
ADMIN_PASSWORD=TestPass123!
